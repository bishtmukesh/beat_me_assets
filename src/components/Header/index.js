import React from 'react';

import './index.css';
import AppBar from '../AppBar';

const Header = () => {
  return (
    <header className="Header">
	    <AppBar />
    </header>
  );
}

export default Header;